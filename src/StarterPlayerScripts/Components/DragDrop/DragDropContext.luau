-- React
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local React = require(ReplicatedStorage.Packages.React)

-- DragDropContext
local DragDropContext = React.createContext(nil)

return DragDropContext